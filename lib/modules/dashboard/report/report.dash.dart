import 'package:flutter/material.dart';

class ReportDash extends StatelessWidget {
  static const String routeName = '/report';

  @override
  Widget build(BuildContext context) {
    return Text('report');
  }
}
